# AI Browser Automation Agent

> Этот проект стал для меня настоящим вызовом и увлекательным путешествием в мир автономных AI-агентов. Изначально я начал реализацию на Go, но столкнулся с серьезными проблемами производительности и сложностью работы с динамическими элементами интерфейса, особенно с теми самыми "синими кнопками" на hh.ru, которые не хотели определяться как кликабельные. После долгих поисков решений и изучения лучших практик в интернете (так как Cursor не всегда мог помочь с такими специфическими задачами), я переписал проект на Python, что дало значительный прирост скорости и более гибкую работу с DOM через Chrome DevTools Protocol. Результат - полностью автономный агент, способный выполнять сложные многошаговые задачи в браузере без хардкода селекторов и предопределенных сценариев.

## 🎯 О проекте

Автономный AI-агент для автоматизации браузера, использующий большие языковые модели (LLM) для принятия решений и выполнения задач. Агент работает через Chrome DevTools Protocol (CDP), анализирует DOM и скриншоты страниц, принимает решения на основе контекста и выполняет действия без предопределенных селекторов или сценариев.

### Ключевые особенности

- 🤖 **Полностью автономный**: Агент сам анализирует страницу и принимает решения
- 🎨 **Vision-модель**: Использует скриншоты для понимания визуального контекста
- 🔄 **Поддержка SPA**: Корректно работает с одностраничными приложениями (React, Vue и т.д.)
- 💾 **Persistent sessions**: Сохраняет cookies и localStorage между запусками
- 🧩 **Архитектура sub-agents**: Специализированные агенты для разных типов задач (например, EmailSubAgent)
- 🔌 **Мультипровайдерность**: Поддержка OpenAI (GPT-4o, GPT-4o-mini) и Anthropic (Claude Sonnet, Claude Opus)
- 🛡️ **Без хардкода**: Никаких предопределенных селекторов, URL или сценариев
- 🎯 **Умное определение элементов**: Использует `is_clickable` из DOMSnapshot для точного определения интерактивных элементов

## 🏗️ Архитектура

### Основные компоненты

```
agent/
├── agent/              # Основной агент и планировщик
│   ├── service.py      # Главный цикл агента (Plan-and-Execute)
│   ├── prompts.py      # Системные промпты и контекст
│   └── message_manager/ # Управление историей и контекстом
├── browser/            # Управление браузером через CDP
│   ├── session.py      # CDP сессия и управление браузером
│   └── watchdogs/      # Обработчики событий (клики, ввод, навигация)
├── dom/                # Работа с DOM
│   ├── service.py      # Построение DOM дерева из CDP
│   └── serializer/     # Сериализация DOM для LLM
├── llm/                # Интеграция с LLM провайдерами
│   ├── openai/         # OpenAI API
│   └── anthropic/      # Anthropic API
├── tools/              # Инструменты агента (click, input, navigate и т.д.)
└── subagents/          # Специализированные sub-агенты
    └── email_subagent.py  # Логика для работы с почтовыми клиентами
```

### Как это работает

1. **Инициализация**: Агент получает задачу и создает браузерную сессию через CDP
2. **Анализ состояния**: 
   - Получает DOM дерево через `DOM.getDocument` и `DOMSnapshot.captureSnapshot`
   - Делает скриншот страницы
   - Сериализует DOM в компактный формат для LLM
3. **Принятие решения**: LLM анализирует контекст (DOM + скриншот + история) и выбирает действие
4. **Выполнение**: Агент выполняет выбранное действие (click, input, navigate и т.д.)
5. **Обновление контекста**: Сохраняет результат в историю и переходит к следующему шагу
6. **Завершение**: Агент завершает задачу когда LLM возвращает `done: true`

### Инструменты агента

- `navigate` - Переход на URL
- `click` - Клик по элементу (по индексу или координатам)
- `input` - Ввод текста в поле
- `extract` - Извлечение информации со страницы
- `scroll` - Прокрутка страницы
- `wait` - Ожидание
- `click_text` - Клик по видимому тексту (для элементов без индекса)
- `click_role` - Клик по элементу с ARIA ролью
- `request_user_input` - Запрос помощи у пользователя (для CAPTCHA, логинов и т.д.)

## 📦 Установка

### Требования

- Python 3.11+
- Chrome/Chromium браузер
- API ключ от OpenAI или Anthropic

### Установка зависимостей

```bash
pip install -e .
```

Или через pip напрямую:

```bash
pip install aiohttp anyio bubus httpx pydantic python-dotenv typing-extensions uuid7 openai anthropic cdp-use pillow markdownify
```

### Настройка

Создайте файл `.env` в корне проекта:

```env
# Выберите один из провайдеров
OPENAI_API_KEY=your_openai_key_here
OPENAI_MODEL=gpt-4o  # или gpt-4o-mini

# Или
ANTHROPIC_API_KEY=your_anthropic_key_here
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929  # или claude-opus-4
```

## 🚀 Использование

### Интерактивный режим

```bash
python main_agent.py
```

Агент запустится в интерактивном режиме, где вы сможете вводить задачи.

### Прямой запуск с задачей

```bash
python run_agent.py "Прочитай последние 10 писем в Яндекс.Почте и удали спам"
```

Или через `main_agent.py`:

```bash
python main_agent.py
# Введите задачу при запросе
```

### Параметры запуска

```bash
# С сохранением сессии
python main_agent.py --session my_session

# В headless режиме (невидимый браузер)
python main_agent.py --headless

# Комбинация параметров
python main_agent.py --session hh --headless
```

## 📝 Примеры задач

### Работа с почтой

```
Прочитай последние 10 писем в Яндекс.Почте и удали спам из них
```

### Работа с вакансиями

```
Изучи моё резюме в профиле на hh.ru. Затем найди 3 подходящие вакансии AI-инженера и откликнись на них с сопроводительным письмом
```

### Универсальные задачи

```
Найди на сайте example.com контактную информацию и сохрани её
Открой google.com, найди информацию о Python и расскажи что нашёл
```

## 🔧 Технические детали

### Работа с DOM

Агент использует Chrome DevTools Protocol для получения полной информации о DOM:

- **DOM.getDocument** - Получение структуры DOM
- **DOMSnapshot.captureSnapshot** - Получение снапшота с информацией о кликабельности элементов (`is_clickable`)
- **DOM.getBoxModel** - Получение координат элементов
- **Runtime.evaluate** - Выполнение JavaScript для сложных взаимодействий

### Сериализация DOM

DOM сериализуется в компактный формат для LLM:

- Интерактивные элементы получают индексы `[i_123]`
- Показываются только ключевые атрибуты (id, class, role, data-qa и т.д.)
- Текст элементов агрегируется из всех вложенных узлов
- Элементы с `is_clickable=True` всегда включаются, даже если невидимы (для модальных окон)

### Sub-agents

Архитектура sub-agents позволяет добавлять специализированную логику для разных типов задач:

- **EmailSubAgent**: Определяет почтовые клиенты, извлекает метаданные писем, предоставляет контекст о структуре почтового интерфейса

### Обработка SPA

Для корректной работы с одностраничными приложениями:

- Ожидание после действий (2 секунды для кликов, 0.5 секунды для навигации)
- Очистка кэша DOM после действий
- Определение изменений через сравнение скриншотов и DOM

### Безопасность

- Токены API хранятся в `.env` файле (не коммитятся в репозиторий)
- Поддержка `request_user_input` для чувствительных операций (логины, CAPTCHA)
- Автоматическое удаление `target="_blank"` для предотвращения открытия новых вкладок

## 🎓 Принципы работы

### Без хардкода

Агент не содержит:
- ❌ Предопределенных селекторов
- ❌ Хардкодных URL
- ❌ Пошаговых сценариев
- ❌ Специфичных подсказок для конкретных сайтов

Вместо этого:
- ✅ Агент анализирует DOM и скриншоты
- ✅ Использует общие знания о веб-интерфейсах
- ✅ Принимает решения на основе контекста
- ✅ Адаптируется к любому сайту

### Автономность

Агент способен:
- Самостоятельно находить нужные элементы на странице
- Планировать многошаговые задачи
- Обрабатывать ошибки и пробовать альтернативные подходы
- Запрашивать помощь у пользователя только когда необходимо

## 🐛 Известные ограничения

- Некоторые сложные React/Vue компоненты могут требовать дополнительных попыток клика
- Модальные окна иногда определяются как невидимые (исправлено через `is_clickable`)
- Очень большие страницы могут замедлять анализ DOM

## 📚 Зависимости

Основные библиотеки:

- `cdp-use` - Работа с Chrome DevTools Protocol
- `openai` / `anthropic` - Интеграция с LLM провайдерами
- `pydantic` - Валидация данных
- `pillow` - Обработка скриншотов
- `aiohttp` / `httpx` - Асинхронные HTTP запросы

## 🤝 Вклад

Проект создан в рамках учебного задания и демонстрирует возможности автономных AI-агентов для автоматизации браузера.

## 📄 Лицензия

Этот проект создан в образовательных целях.

---

**Примечание**: Для работы агента необходим API ключ от OpenAI или Anthropic. Агент использует видимый браузер по умолчанию для лучшей отладки, но поддерживает headless режим.

